{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "connection-setup",
    "description": "Only the bundled Drclick connection page may verify and save a Cloud or Cabinet Hub endpoint.",
    "local": true,
    "windows": ["main"],
    "permissions": [
        "allow-probe-server-connection",
        "allow-configure-server-connection"
    ]
}
