{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "desktop-windows",
    "description": "Only the supervised loopback application window may invoke the content-bound restore, LAN configuration, adapter inspection, and Google OAuth browser commands; no generic opener, shell, or filesystem capability is granted.",
    "local": false,
    "remote": {
        "urls": ["http://127.0.0.1:*/*"]
    },
    "windows": ["main"],
    "permissions": [
        "allow-apply-prepared-offline-restore",
        "allow-open-google-oauth-authorization",
        "allow-list-lan-adapters",
        "allow-apply-lan-listener-configuration",
        "allow-signed-updater-status",
        "allow-check-for-signed-update",
        "allow-install-signed-update"
    ]
}
